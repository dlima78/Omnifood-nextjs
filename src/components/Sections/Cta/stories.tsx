import { Meta, StoryObj } from '@storybook/react'
import Cta from '.'

export default {
  title: 'Cta',
  component: Cta,
  parameters: {
    layout: 'fullscreen',
    backgrounds: {
      default: 'dark'
    }
  }
} as Meta

export const Default: StoryObj = {}
