import { Meta, StoryObj } from '@storybook/react'
import How from '.'

export default {
  title: 'How',
  component: How,
  parameters: {
    layout: 'fullscreen',
    backgrounds: {
      default: 'dark'
    }
  }
} as Meta

export const Default: StoryObj = {}
